package demo

import (
	"testing"
)

func TestName(t *testing.T) {
	//a := conf.NewReadYaml().GetBytesByPath("config.yaml")
	//a, err := doc.NewCmdClient().ReadDocByPackageName("github.com/quiet-xu/goburnt/demo")
	//t.Log(a, err)
	//var x FView
	//stValue := reflect.ValueOf(x)
	//num := stValue.NumMethod()
	//for i := 0; i < num; i++ {
	//	name := reflect.TypeOf(x).Method(i).Func
	//	path := reflect.TypeOf(dst).PkgPath()
	//	s.methodPathMap[path] = append(s.methodPathMap[path], name)
	//}
	return
}
